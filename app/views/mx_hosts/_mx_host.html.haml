%tr
  %td= mx_host.hostname
  %td= mx_host.address
  - if mx_host.starttls
    %td= mx_host.tls_version
    %td{ class: mx_host.cert_valid ? 'success' : 'danger' }= mx_host.cert_valid ? 'valid' : 'invalid'
  - else
    %td{colspan: 2} missing
