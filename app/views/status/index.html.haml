%h1 Status

%h2 Processors

.row
  - @status.each do |key,value|
    .col-md-6
      %h3 #{key} processor
      %pre~ value.to_yaml

%h2 Cache Entries

%h3 MX Cache
%pre~ @cache_mx.to_yaml

%h3 Host Cache
%pre~ @cache_hosts.to_yaml
